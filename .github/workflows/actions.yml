name: all-pull-request
on: [push]
jobs:
  build_test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v7
      - uses: actions/checkout@v7
      - run: npm i
      - run: npm run lint
      - run: npm run build
